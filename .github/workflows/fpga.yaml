name: fpga

on:
  push:
  workflow_dispatch:

jobs:
  fpga:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: FPGA bitstream for TT ASIC Sim (ICE40UP5K)
        uses: chipfoundry/tt-gds-action/fpga/ice40up5k@03c4904aa3e0ccfcaf38247b86eb9c3317c15cd2
